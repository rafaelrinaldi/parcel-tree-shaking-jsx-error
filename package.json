{
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "parcel build index.html -d build --no-cache --experimental-scope-hoisting",
    "prestart": "npm run build",
    "start": "live-server build --no-browser"
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-2": "^6.24.1",
    "live-server": "^1.2.0",
    "parcel-bundler": "^1.9.5"
  },
  "dependencies": {
    "history": "^4.7.2",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-loadable": "^5.4.0",
    "react-router": "^4.3.1",
    "react-router-dom": "^4.3.1"
  }
}
